!======================================================================!
subroutine grow
!----------------------------------------------------------------------!
use params
use vars
!----------------------------------------------------------------------!
implicit none
!----------------------------------------------------------------------!
! Need to see if OK. Needs Ed?
!----------------------------------------------------------------------!
fT = exp ((Eag / k) * ((one / T0g) - (one / tmp))) / &
     exp ((Eag / k) * ((one / T0g) - (one / (25.0 + tf))))
!----------------------------------------------------------------------!
if ((swp_crit < swp) .and. (swp <= 0.0)) then
  fW = (swp_crit - swp) / swp_crit !es21
else
  fW = 0.0
end if
!----------------------------------------------------------------------!
if (AWG) then
  NPP = fT * fW * 3000.0 / (4.0 * 365.0 * dt)
  !NPP = fW * 3000.0 / (4.0 * 365.0 * dt)
  !NPP = (sm/SM_MAX) * 3000.0 / (4.0 * 365.0 * dt)
else
  NPP = zero
end if
  !NPP = fT * (sm/SM_MAX) * 3000.0 / (4.0 * 365.0 * dt)
  !NPP = fT * fW * 3000.0 / (4.0 * 365.0 * dt)
!----------------------------------------------------------------------!
end subroutine grow
!======================================================================!
